# Опционально. Vite подхватывает при npm run dev.
# Auth и calc — внешний сервис. Same-origin proxy сохраняет cookies.

# AUTH_PROXY_TARGET=http://localhost:3005
# CALC_PROXY_TARGET=http://localhost:3005
# BACKEND_PROXY_TARGET=http://localhost:3007

# Если локальный :3005 без нужных шифров — staging:
# AUTH_PROXY_TARGET=https://dev3.constrtodo.ru:3005
# CALC_PROXY_TARGET=https://dev3.constrtodo.ru:3005
